#include<stdio.h>
#include<string.h>

int main(){
	int n = 40;
	char sttr[n];//here n is variable which is must to define while taking input in an empty character array
	// puts(sttr);
	// scanf("%s",sttr);
	gets(sttr);
	printf("%s",sttr);
}