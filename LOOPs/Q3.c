//wap to print out all armstrong numbers betw 1 and 500, if sum of cubes of each digit of the number is equal to the number itself, then the number is called armstrong number for example,153 = (1*1*1)+(5*5*5)+(3*3*3)
#include<stdio.h>
int main(){
      
    return 0;
}